# Copy to /srv/orderdesk/.env on the server. Never commit the real one.
IMAGE=ghcr.io/ensostudiolt/sample-python-docker-ci
IMAGE_TAG=latest
POSTGRES_PASSWORD=change-me
# Hostname for automatic HTTPS via Caddy. Leave as :80 until DNS points here.
SITE_ADDRESS=:80
